/* Create a relation Books, with the following columns: book id (integer),
title (varchar (300)), author (varchar(100)), date of publish (date) */
CREATE TABLE books(
id INTEGER,
title VARCHAR(30),
author VARCHAR(30),
date_of_publish DATE)